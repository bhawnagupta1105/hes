// import React, { useState } from "react";
// import InvoiceGrid from "./InvoiceGrid";
// import ReportWithPreview from "./ReportWithPreview";

// export default function InvoicePage({ cardTitle }) {
//   const [showReport, setShowReport] = useState(false);
//   const [selectedRowData, setSelectedRowData] = useState(null); // State to store the clicked row data

//   // Function to open report and set the selected row data
//   const handleOpenReport = (rowData) => {
//     setSelectedRowData(rowData);
//     setShowReport(true);
//   };

//   return (
//     <div>
//       <InvoiceGrid cardTitle={cardTitle} onTitleClick={handleOpenReport} />
      
//       {showReport && selectedRowData && ( // Only show if report is open and row data is available
//         <ReportWithPreview rowData={selectedRowData} onClose={() => setShowReport(false)} />
//       )}
//     </div>
//   );
// }